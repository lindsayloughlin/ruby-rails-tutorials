<% provide(:title, @user.name) %>
<h1>
	<%=gravatar_for @user %>
	<%= @user.name %>
</h1>

<div class="span8">
	<% if @user.microposts.any? %>
		<h3>microposts (<%= @user.microposts.count %>) </h3>
		<ol class="microposts">
			<%= render @microposts %>
		</ol>
	<% end %>
</div>